// testcase: calculateMagic|5-12

const a = 123;

/**
 * Calculates magic number from `input` string.
 * @param input - magic string
 * @returns calculated magic special number value.
 */
function calculateMagic(input: string): Promise<number> {
    return Promise.resolve(123);
}

const b = '123';

function isEmpty(val: string): boolean {
    return true;
}